# Markdown is hand-written prose; oxfmt reflows it and fights the writing.
*.md
# Key order in package.json is deliberate.
package.json
# Fixtures are generated by scripts/gen-fixtures.py and compared byte for byte.
test/fixtures/*.json
